# Production Environment Variables
# Copy this file to .env in production and fill in the values

# Server Configuration
NODE_ENV=production
PORT=3000

# Database Configuration (PostgreSQL)
# Option 1: Use DATABASE_URL (Recommended for managed databases)
DATABASE_URL=postgresql://user:password@host:5432/movies_db

# Option 2: Use individual variables (if DATABASE_URL is not set)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=movies_db
DB_USER=your_db_user
DB_PASSWORD=your_secure_password

# CORS Configuration (Optional - comma separated origins)
# CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com
